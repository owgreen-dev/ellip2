# Ralph Progress Log — ellip2 (Bitcoin/Elliptic2 AML)

Cross-session memory. The fresh-context loop reads this each iteration. Append what you
learned; keep it short and high-signal.

## Project orientation (read once)

- **Master spec:** `plan.md` (staged pipeline §9, repo layout §6, Resolved design
  decisions near the top). Tasks live in `plans/prd.json`. Rules in `plans/guardrails.md`.
- **Goal:** subgraph-level AML detection on Elliptic2 + cluster-level PU, then exit-path
  discovery + an LLM typology/report layer.
- **Verify gate:** `bash scripts/verify.sh` = pytest + ruff + mypy, all via `.venv`.
- **Env:** no system pip — use `.venv/bin/python` and `uv`. CPU only (no GPU). Tests are
  synthetic/mocked (see `tests/test_ingest.py`, `tests/test_splits.py`).

## Done before the loop started

- **Stage 0 ingest** (`src/ellip2/data/ingest.py`, `schema.py`): DuckDB out-of-core →
  `id_map.parquet`, `node_features.npy`, `edge_index.npy`, `subgraphs.parquet`,
  `ingest_manifest.json`. Integrity checks. Tested (`tests/test_ingest.py`).
- **Split generator** (`src/ellip2/eval/splits.py`, `splits_cli.py`): persisted
  subgraph-level train/val/test (stratified_random default; round_robin reproduces
  GLASS). Tested (`tests/test_splits.py`).
- Baseline is **green**: 13 tests, ruff clean, mypy clean.

## Stage → modules still to build (see prd.json for the ordered backlog)

- **Stage 1 features** → `src/ellip2/features/{degree,edge_aggs,flow_concentration,neighborhood,temporal,path_role,build}.py`
- **Stage 2 graph+PU** → `src/ellip2/graph/{pyg_data,neighbor_sampling}.py`,
  `src/ellip2/pu/{nnpu_loss,prior_estimation,encoder,trainer}.py`,
  `src/ellip2/eval/{pu_metrics,leakage_checks}.py`
- **Stage 3 discovery** → `src/ellip2/exit_paths/path_search.py`, `scripts/discover.py`
- **LLM + report** → `src/ellip2/llm/{serialize_subgraph,bedrock_client,typology_graph}.py`,
  `src/ellip2/report/render.py`
- **Config** → `configs/` (Hydra). **Skipped:** infra Stages 1-3, real-data e2e.

## Iteration notes

<!-- Append: [T-00X] one line on what was built, key decision, any gotcha. -->

- [T-032] **LICENSE (MIT) + non-commercial data note** — new `LICENSE` (standard MIT text, year 2026, holder `owgreen-dev` from git identity) + new `DATA.md`. DATA.md restates the dataset facts (Kaggle `ellipticco/elliptic2-data-set`, ~24 GB compressed / **~83 GB extracted**, paper Table 1 counts) and the **data-vs-code split**: dataset is **CC BY-NC-ND 4.0** (non-commercial, no-derivatives, NOT redistributed, separate from the code license); the **code is MIT** (points at `LICENSE`). README's Data section already carried the CC BY-NC-ND note + DATA.md link from T-031, so no README edit was needed. **`tests/test_docs.py` (T-032 slice, appended):** `test_license_exists_and_is_mit` (LICENSE is a file AND contains `'MIT'`) + `test_data_noncommercial_note_present` (any of README.md/DATA.md contains `'CC BY-NC-ND'` OR `'non-commercial'`). 295 green (was 293), ruff+mypy clean. **Next (T-033):** RESULTS.md — detection-progression table (0.030→…→0.942) + NAMED baseline table (RevTrack Table 1: RevClassify_DS 0.974/0.953 · ours 0.942/0.854 · GLASS 0.816/0.705) + discovery recovery (208 candidates, 5/276=1.8%, 121× lift) + Limitations; test asserts RESULTS mentions RevClassify+GLASS, has caveats, and `0.942` appears in BOTH README and RESULTS.

- [T-031] **README.md** — the project front door + `tests/test_docs.py` (first publishing-doc task; T-032..T-036 will EXTEND the same test file). All numbers copied verbatim from `plans/publishing_facts.md` (single source of truth — do NOT invent metrics). **README ## sections** (exact headers the test greps): `Overview, Results, Pipeline, Quickstart, Data, Reproduce, Repo layout, Limitations, License, Citation`. Results has BOTH the detection-progression table (0.030→0.286→0.816→0.844→**0.942**) and the NAMED baseline table (RevTrack Table 1: RevClassify_DS 0.974/0.953 · ours 0.942/0.854 · GLASS 0.816/0.705) — states the different-split caveat. Pipeline is an ASCII Stage 0→Discovery diagram. Data says Kaggle `ellipticco/elliptic2-data-set`, **~24 GB compressed / ~83 GB extracted**, **CC BY-NC-ND 4.0** non-commercial + NOT redistributed. Links `RESULTS.md`, `DATA.md`, `LICENSE`, `RUNBOOK.md` (those files land in T-032/T-033 — links are just strings, fine now). **`tests/test_docs.py`** (T-031 slice): `REPO_ROOT=parents[1]`, `README_SECTIONS` tuple; 4 tests — README exists, has every `## ` header, contains `'83'` and NOT `'26 GB'`/`'26GB'`, links `RESULTS.md`. 293 green (was 289), ruff+mypy clean. **Next (T-032):** LICENSE (MIT, holder owgreen-dev) + DATA.md non-commercial note; extend test_docs.py with LICENSE/data-note asserts.

- [T-030] **Held-out-recovery proxy eval harness** — the FINAL non-skipped task. New `discovery/eval_recovery.py` + `scripts/eval_recovery.py` shim. **`recovery_rate(discovered_member_sets, target_member_sets, n_nodes) -> RecoveryResult`**: a target subgraph is *recovered* iff ≥1 of its member clusters lands in the UNION of the discovered member sets; `recovery_rate = n_recovered/n_targets`. **Random baseline** = expected recovery of a size-`D` set (`D=|discovered union|`) drawn uniformly from `N`: per-target hypergeometric complement `1 - Π_{i<m}(N-D-i)/(N-i)` (chance none of the `m` members is drawn), averaged over targets; guaranteed-hit short-circuit when `m > N-D` → 1.0, empty target → 0.0. `RecoveryResult` also carries `discovered_union_size`, `n_nodes`, `baseline_rate`, `lift`(=rate/baseline; `inf` when baseline 0 & rate>0, else 0.0). Member arrays are clipped to `[0,N)` so a spurious huge idx can't inflate the union. **CLI `main`** reads `discovered_subgraphs.parquet` (`member_idx`) + `subgraphs.parquet` + `split.csv` + `--n-nodes`, takes the `--eval-split`(default test) SUSPICIOUS subgraphs as targets (`suspicious_targets` filters `ccLabel==suspicious` ∧ ccId∈split via a local `_allowed_ccids`), prints rate/baseline/lift. **Split-filtered exclusion (2nd deliverable):** `background.known_member_idx` gained `split_csv`/`split_name` kwargs — when BOTH given it only unions members of subgraphs in that split (reads ccId+member_idx, filters via a background-local `_allowed_ccids`), so `discover_main --split-csv … --exclude-split train` excludes ONLY train members and leaves held-out (test-suspicious) clusters seedable; default None→None keeps the old unfiltered union (back-compat). `discover_main` added `--split-csv`/`--exclude-split` (errors if exclude-split without split-csv). **The proxy loop:** cluster scorer trains on train-split members (test held out) → run discovery excluding only train members → measure how many test-suspicious subgraphs discovered candidates recover vs random; label-free yardstick, real eval RUN is a manual box step. **Test (`tests/test_eval_recovery.py`, SIGN-101):** hand-checked recovery+baseline (N=10, union{0,1,2,3}, targets recovered=2/3, baseline=(2/3+2/3+0.4)/3), guaranteed-hit `_baseline_hit_prob(2,4,5)==1`, empty-discovered→baseline 0/lift 0, no-targets, out-of-range clip, n_nodes≤0 raises, split-scoped `suspicious_targets` (only test-suspicious), split-filtered `known_member_idx` excludes ONLY the named split (train→{0,1}, unfiltered→all) + requires-both-args back-compat, CLI end-to-end. 289 green (was 279), ruff+mypy clean. **ALL non-skipped tasks now pass** — backlog empty except T-023/T-024 (skip:true AWS/GPU integration).

- [T-029] **Optimized per-candidate reachability** — killed the ~500× re-transpose of the 196M-nnz background graph (the ~49min real-run hot spot). **Root cause:** `bfs_reachable` (`exit_paths/path_search.py`) did `step = adjacency.transpose().tocsr()` on EVERY call, and `candidate_member_sets` calls it once per candidate → the O(nnz) transpose recurred per candidate. **Fix (API-back-compatible):** `bfs_reachable` gained an optional keyword `step_matrix: sparse.spmatrix | None = None` — when supplied it MUST equal `adjacency.transpose().tocsr()`; the fn then skips its internal transpose (path_search.py:198) and uses it directly (shape-checked vs adjacency, else ValueError). Default None → identical old behavior, so `reachability`/all existing callers are untouched. **`candidate_member_sets` (`discovery/background.py`)** already built both `a` (CSR) and `a_t = a.transpose().tocsr()` once; now it PASSES them as step matrices: the global backward sweep walks `a_t`'s out-edges so its step matrix == `a` (`step_matrix=a`); every per-candidate forward sweep walks `a`'s out-edges so its step matrix == `a_t` (`step_matrix=a_t`). Net: exactly ONE transpose per `candidate_member_sets` call (building `a_t`), independent of candidate count. **Key insight:** the forward step matrix (`a_t`) is the SAME matrix already built for the backward sweep — no extra work, just reuse. **Tests:** (path_search) `bfs_reachable` with `step_matrix` == without, across seed sets × frontier caps (reached/hops/capped_levels identical); shape-mismatch raises. (background) `candidate_member_sets` output IDENTICAL to an inline pre-T-029 reference (`_carve_without_opt`, never passes step_matrix); **structural transpose-count assertion** — monkeypatch `scipy.sparse.csr_matrix.transpose` with a counter, assert count with 1 candidate == count with 3 candidates (pre-fix it was 1+N, so this catches any regression). 279 green (was 275), ruff+mypy clean. **Next (T-030):** held-out-recovery proxy eval harness (`discovery/eval_recovery`, `recovery_rate` + random baseline, split-filtered known-member exclusion).

- [T-028] `discovery/background.py` **`discover_background(...)`** orchestrator + `write_discovered` + `discover_main` CLI + `scripts/discover_subgraphs.py` shim — the FINAL non-skipped task (plan `yes-lets-do-it-glittery-coral.md` task 4). Wires the three-gate funnel over background clusters: **Gate1** `threshold=quantile(scores, score_percentile)`, eligible = `(scores>=threshold) & ~known_mask` (known_members excluded), top-K by descending score (eligible is ascending → stable `argsort(-s)` breaks ties by ascending id); **Gate2** per-candidate carve via **T-027 `candidate_member_sets`** (reuses the global-backward-BFS + per-candidate-forward-BFS reachability) — a candidate passes iff its carve is NON-EMPTY (reaches an endpoint within max_hops); **Gate3** `typology_signal[cand] >= typology_threshold` (typology_signal=None → all-zero, no-op). Survivors' carved member sets are **border-scored ONE AT A TIME** with a trained node-only `SupervisedSubgraphModel`: `extract_border_sets(edge_index, [member sets], n_nodes, cap)` (one edge pass), then loop `build_subgraph_batch([j], border, node_features, mean/std, edge_dim)` → `sigmoid(model(batch)).item()` per survivor (score_border.py template). **Ranked** by descending border score, ties by ascending candidate id. **Output schema (reuse-unchanged, THE requirement):** `write_discovered` emits `discovered_subgraphs.parquet` (`ccId,ccLabel,n_members,member_idx` — SAME as `subgraphs.parquet`) + `discovered_scores.parquet` (`ccId,score,label,split` — SAME as `*_scores.parquet`); novel finds have no ground truth so `ccId="bg<cand>"`, `ccLabel="unknown"`, `label=-1`, `split="discovered"` — so Stage-4 `report.leads.rank_leads`/`investigate` consume discovered structures with no code change. `BackgroundDiscoveryConfig` (score_percentile=0.9, top_k=1000, max_hops=MAX_HOPS, frontier_cap, typology_threshold=0.0, border_cap=64); `DiscoveredSubgraph`(cc_id, candidate, member_idx, cluster_score, typology_signal, border_score, rank); `BackgroundDiscoveryResult`(discovered, score_threshold, n_candidates, n_reached). `discover_main` loads cluster_scores.npy/edge_index.npy/node_features.npy(mmap)/endpoints.npy/typology_signal.npy + rebuilds the model from a border checkpoint's `extra` dict (node_dim/edge_dim/set_hidden/set_out/mlp_hidden/feat_mean/feat_std/border_cap — same as score_border) and pulls known members via `known_member_idx(subgraphs)`. **Design/gotchas:** heavy imports (torch, border_assembly, trainer) are LAZY inside the fns so the light T-026/T-027 helper path (make_typology_signal shim) doesn't pull torch; model type-hinted via `TYPE_CHECKING` import (annotations are strings under `from __future__`). **Test gotcha (the threshold trap):** with mostly-zero scores, `score_percentile=0.5` → threshold=0 (the MEDIAN is 0) → admits every low-score node as a candidate; used `score_percentile=0.7` so only the four 0.9-seeds qualify. **Test (10-node graph, endpoint 9, SIGN-101):** seeds 0/3/5/7 all score 0.9 — 0 reaches+strong-typology→SURFACES(bg0, members [0,1,2], endpoint 9 dropped); 3 reaches but weak typology→Gate3 drop; 5 reaches but is a known member→Gate1 drop; 7 no path→Gate2 drop. Asserts discovered==[0], n_candidates=3, n_reached=2, known never appears, typology-optional path (no signal→{0,3}), known-excluded-even-with-top-score (scores[5]=5, top_k=1→picks 0 not 5), schema round-trip, and a full `discover_main` end-to-end writing/reading both parquets from a saved checkpoint. 275 green (was 270), ruff+mypy clean. **ALL non-skipped tasks now pass** — backlog is empty except T-023/T-024 (skip:true AWS/GPU integration).

- [T-027] `discovery/background.py` **`candidate_member_sets(edge_index, candidate_ids, endpoints, n_nodes, *, max_hops=MAX_HOPS, frontier_cap, hubs) -> {int: (M,) int64}`** — the per-candidate reachability CARVE (plan `yes-lets-do-it-glittery-coral.md` task 3). **Reuses the Stage 3 BFS** by importing the private helpers from `exit_paths.path_search` (`MAX_HOPS, _as_node_ids, _build_directed_adjacency, _hub_mask, bfs_reachable`) — same meet-in-the-middle carve as `path_search.reachability` but specialised to ONE source and run per candidate. **Cost split (the whole point):** adjacency `a` built once; the **global backward BFS** from the endpoint set (`bfs_reachable(a.T, endpoints)`) is computed ONCE and reused for every candidate (its result is candidate-independent); only the cheap **forward BFS** (seeded from the single candidate on `a`) runs per candidate. Survivors = `fwd.reached & back.reached & (fwd.hops+back.hops <= max_hops) & ~drop_endpoint` → `np.nonzero(...)` sorted int64. **Endpoint ids dropped** from every member set (target, not part of the discovered structure); a candidate reaching no endpoint within `max_hops` → **empty** array (both.reached fails at the candidate itself). Candidates deduped via `_as_node_ids`. Same edge_index validation as `reachability` (shape (2,E), in-range, E==0 ok). **Test (shared `tests/test_background_discovery.py`, SIGN-101):** graph `0→1→2→3`(endpoint) + dead-end `1→4`; `_edge_index` helper from (src,dst) list. carve of cand 0 = `{0,1,2}` (endpoint 3 dropped, off-path 4 excluded because backward-unreached); max_hops=2 → cand 0 (3 hops away) empty, cand 1 (2 hops) = `[1,2]`; dedup `[0,0]`→one key; bad-shape (3,4) raises. 270 green (was 265), ruff+mypy clean. **Next (T-028):** `discover_background` orchestrator wiring Gate1(score/exclude-known/top-K)→carve(T-027)→Gate2/3→one-at-a-time border score→rank→parquet.

- [T-026] `discovery/background.py` (new module — T-027/T-028 will extend it) + `scripts/make_typology_signal.py` shim: the two small background-discovery helpers (plan `yes-lets-do-it-glittery-coral.md` task 2). **`typology_signal_from_features(features_parquet, *, score_col="source_sink_axis") -> (N,) float64`** = Gate-3 structural signal, mirrors `exit_paths.recover.endpoints_from_features`: reads `idx`+`source_sink_axis` (T-006 `path_role`), sorts by `idx`, **asserts contiguous 0..N-1** (raises otherwise) so the array is positional/idx-aligned. `main()` writes it to `typology_signal.npy`. **`known_member_idx(subgraphs_path, *, n_nodes=None) -> (M,) int64`** = the exclusion set: `np.unique(concat(member_idx))` over all rows (sorted-unique union; overlaps collapse), optional `[0,n_nodes)` bound — Gate 1 drops these already-known clusters so discovery ranks only NOVEL structures. Both pure/offline (SIGN-101), lazy `import pyarrow.parquet` inside the fns (idiom). **Test (`tests/test_background_discovery.py`, shared with T-027/T-028):** writes features with rows in a **shuffled** idx order (`[2,0,4,1,3]`) → asserts the recovered signal is per-idx aligned (can't pass by accident); custom score_col; non-contiguous idx raises; union correct on overlapping/out-of-order member lists `[[0,1,2],[2,3],[7,5]]`→`[0,1,2,3,5,7]`; `n_nodes=5` bound drops 5,7; `main` round-trips the npy. 265 green (was 259), ruff+mypy clean. **Next (T-027):** `candidate_member_sets` — per-candidate reachability carve (global backward BFS from endpoints reused + per-candidate forward BFS), in this same module.

- [T-025] `pu/cluster_score.py` + `scripts/{train_cluster,score_cluster}.py` shims: the background-discovery **Gate-1 per-cluster suspicion scorer** (plan `yes-lets-do-it-glittery-coral.md` task 1). Replaces the ~random cluster-nnPU (test PR-AUC 0.03) with a **grounded supervised HGBM** — same estimator/pattern as `train_subgraph.py`. **Label construction (reuses `pu.train`):** positives = `positive_mask(split)` (suspicious-subgraph members); the new `labeled_member_mask(subgraphs, N, split_csv, split_name)` = complement WITHOUT the suspicious filter (any labeled member, licit OR suspicious, split-scoped via the reused private `_allowed_ccids`); train rows = `flatnonzero(labeled)`, `y = positive[rows]` (positive precedence on overlap). Negatives are thus bona-fide licit members. Class-weight rare positives `neg/pos` like train_subgraph. **`train_main`** fits on train-split labeled members, reports **test-member PR-AUC** via `pu_metric_report` (guarded to only eval when the eval split has both classes), pickles `{model, framing="cluster_suspicion_hgbm", feature_names}`. **`score_main`** validates file feature-cols == training cols, then `predict_cluster_scores` **streams the parquet in row batches** (`ParquetFile.iter_batches(batch_size=1M, columns=["idx"]+feats)`) placing each row's `predict_proba[:,1]` by its **`idx`** key → `(N,)` `cluster_scores.npy` (SIGN-104 lean-load: the real feature table decompresses to ~17GB, never materialized whole; also robust to file row-order). Raises if any idx left NaN (non-contiguous idx). **Test (synthetic, SIGN-101):** 300 clusters (100 labeled = 20 subgraphs×5, +200 background), suspicious members +3 / licit −3 → separable; asserts `labeled_member_mask` is split-scoped + never marks background, then train→score end-to-end: `scores.shape==(N,)`, all in [0,1], held-out test-member `pr_auc>0.9` and suspicious.min() > licit.max(). Two entry-point mains (train/score) in one module, each with its own shim — mirrors train_subgraph/score_subgraph. 259 green (was 257), ruff+mypy clean. **Next (T-026):** typology-signal export (`source_sink_axis`→.npy) + known-member exclusion union helper.

- [T-022] `configs/` Hydra config tree + `tests/test_configs.py` — the LAST non-skipped task (T-023/T-024 are skip:true integration). **Decision: NO new dependency.** hydra-core/omegaconf are not installed; the acceptance criteria explicitly allow the fallback ("validates YAML loads + required keys if hydra is not added"), so I authored the tree as plain composable Hydra-style YAML (a `defaults` list + one option file per group) and the test composes it with **PyYAML** (already in `.venv`, used nowhere else but present) — zero install, fully offline (SIGN-101). **Tree:** `config.yaml` (composition root: `defaults: [dataset:elliptic2, features:default, pu:supervised, gnn:graphsage, llm:bedrock, infra:g5, _self_]` + run scalars seed/run_name/artifacts_dir) over six groups `dataset/ features/ pu/ gnn/ llm/ infra/` (plan.md §6). pu/ ships TWO options — `supervised.yaml` (DEFAULT, weighted_bce + pr_auc, Resolved decision #2) and `nnpu.yaml` (cluster-level PU: small `prior:0.001` + `prior_sweep:[1e-1..1e-5]`, NOT the 2.27% subgraph base rate). Config values mirror the built modules (gnn in_channels=43/num_neighbors=[15,10]; llm model_id=anthropic.claude-sonnet-4-6/max_serialize_bytes=16000/4 typologies; dataset Table-1 counts). **Test (`_compose()` helper):** parses the `defaults` list → loads each `group/option.yaml` under its group key → overlays root scalars (the `_self_` step), then asserts every group resolves + required keys present + typed (8 tests: root exists, every group has a default option, all option files are valid mappings, compose resolves groups+scalars, required-keys-per-group, nnpu prior/descending-sweep grid, base-rate sanity 2763/121810≈0.0227). **Gotcha:** the `(group, option), = entry.items()` single-item unpack needs the trailing comma (tuple target); ruff/mypy clean since test compose is plain dict/pathlib. 233 green (was 226), ruff+mypy clean. **ALL non-skipped tasks now pass — backlog is empty except T-023/T-024 (skip:true, AWS/GPU integration).**

- [T-021] `report/render.py` (new `report/` pkg): renders a per-subgraph investigative LEAD (plan.md §8). **Two pure entry points + one adapter.** `SubgraphReport` (frozen dataclass) is the structured input: `subgraph_id, pu_score, pu_percentile, exit_paths, endpoint_type(heuristic licit endpoint), typology, confidence, structural_evidence(degree/Gini/flow Mapping), rationale, evidence, flagged, validation_reason`. `build_report(report) -> dict` = JSON-able dict with **`caveats` ALWAYS present + non-empty** (the mandatory false-positive block); `render_report(report) -> str` = Markdown with fixed sections (header, PU score+percentile, typology+confidence+FLAGGED marker, `## Exit path(s)` w/ endpoint type, `## Structural evidence` sorted, `## Model rationale`+cited evidence, trailing `## Caveats`). **`CAVEAT_LINES`** (module const tuple) is the load-bearing requirement — every report says "INVESTIGATIVE LEAD not a finding", PU score is a SCAR lower bound, roles/endpoint are DERIVED heuristics, typology is a model verdict, false positives expected → human review. **Adapter** `report_from_classification(candidate, classification, *, pu_percentile, endpoint_type, structural_evidence=None)` bridges the upstream layers: `candidate` is a T-018 `CandidateSubgraph`, `classification` is the dict from T-020 `classify_candidate` (typology/confidence/rationale/evidence/flagged/validation_reason); structural_evidence DEFAULTS to `candidate.stats` (the Stage 1 degree/Gini/flow summary) when not passed; missing optional classification keys default cleanly (`rationale=""`, `evidence=[]`, `flagged=False`, `validation_reason=None`). Pure function, no network/I/O (SIGN-101). **Design choice:** the caveat is a module constant rendered verbatim in BOTH outputs (dict `caveats` list == text `## Caveats` bullets) so the test asserts list-equality + key phrases (investigative lead / human review / false positive) — can't silently drop it. Flagged typology surfaces a `FLAGGED (structural contradiction)` marker in the text (present iff `flagged`). Empty exit_paths → `(none recovered)` placeholder, caveat still mandatory. 8 new tests (all required dict fields, value round-trip incl. degree/Gini/flow, caveat present+phrases, text sections+all caveat lines, flagged marker on/off, empty-paths placeholder, adapter uses candidate.stats, adapter override + optional-key defaults). 226 green (was 218), ruff+mypy clean. **Remaining backlog:** T-022 configs/ (Hydra) is the last non-skipped task.

- [T-020] `llm/typology_graph.py`: the LangGraph agent wiring the LLM typology layer (plan.md §8). **langgraph 1.2.6 IS installed** (`from langgraph.graph import StateGraph, START, END`; no `__version__` attr — use `importlib.metadata.version`). **Graph = 4 linear nodes** `START→serialize→classify→validate→report→END` over a `TypologyState(TypedDict, total=False)` carrying `candidate / serialized / result / validation / report`; each node returns a PARTIAL state dict (LangGraph merges). **The classify node is INJECTABLE (SIGN-101, the whole point):** `build_typology_graph(classifier, *, config=None, override_on_contradiction=True)` takes `Classifier = Callable[[Any], TypologyResult]` — production passes `BedrockTypologyClient(...).classify`, tests pass a `_MockClassifier` returning a canned `TypologyResult`. No network, no boto3. `classify_candidate(candidate, classifier, ...)` is the one-shot convenience (builds graph, `app.invoke({"candidate":...})`, returns the report dict). **Rule-based validation (the override/flag requirement):** `compute_structural_signals(candidate)` derives `StructuralSignals(n_nodes, n_edges, max_in_degree, max_out_degree, linearity=n_edges/(n_nodes-1))` purely from edge degrees (no model/labels); `predict_typology(signals)` maps shape→typology — **hub** (max_in≥2 AND max_out≥2)→`nested_service`; **funnel** (in≥2, out≤1)→`consolidation`; **split** (out≥2, in≤1)→`layering_smurfing`; **near-linear** (in≤1,out≤1, linearity≥0.5)→`peeling_chain`; else `None` (inconclusive, <2 nodes). `validate_typology(result, signals, *, override=True)`→`ValidationOutcome(claimed, structural, agrees, flagged, final_typology, reason)`: structural=None ⇒ accept claim unflagged; agree ⇒ unflagged; **disagree ⇒ flagged**, and `final_typology` = structural reading if override else kept-but-flagged. Report carries `typology`(final) + `model_typology` + `structural_typology` + `flagged` + `validation_reason`. **mypy gotcha:** langgraph SHIPS types (the `langgraph.*` override's ignore_missing_imports doesn't suppress them), and `StateGraph.add_node`'s overloads reject a factory-returned `Callable[[TypologyState], dict]` (call-overload error) — fixed by typing the builder local `graph: Any = StateGraph(TypologyState)` (node signatures still checked at their defs; wiring gets no real safety from constraining it). **Test design (16 tests, all mock LLM):** signal computation per shape (chain/funnel/split), `predict_typology` for all 4 typologies + inconclusive singleton, validation agree/override/flag-without-override/inconclusive, e2e corroborated (asserts classifier called once with the serialized doc — state-transition proof), e2e override-flags-contradiction (model says peeling_chain on a funnel → final overridden to consolidation, flagged), no-override keeps flagged model typology, build-without-invoke doesn't call the LLM, config forwarded, invalid candidate (dup node id) propagates the serializer's ValueError. 218 green (was 202), ruff+mypy clean. **For T-021 report/render:** `classify_candidate(...)` returns the per-subgraph report dict (typology+confidence+rationale+evidence+flagged+validation_reason) the renderer formats.

- [T-019] `llm/bedrock_client.py`: thin wrapper over Bedrock **Converse** that classifies a serialized subgraph's AML typology (plan.md §8). **Injectability is the whole point (SIGN-101):** `BedrockTypologyClient(client=None, config=BedrockConfig())` takes an injectable boto3 `bedrock-runtime` client; a real one is built **lazily** via module-level `build_default_client(config)` (which does `import boto3` INSIDE the fn) only on first `.client` access when none was injected — so constructing the object + calling `classify()` with a stub touches NO network/AWS/boto3. **Pure parsing fns (unit-testable from literals):** `extract_text(resp)` walks `resp["output"]["message"]["content"]` and concatenates `"text"` blocks (ignores tool-use blocks; raises ValueError on missing path / no text block); `parse_converse_response(resp) -> TypologyResult` decodes the assistant text as JSON and validates the 4 required keys. **`TypologyResult`** (frozen): `typology:str, confidence:float (clamped [0,1]), rationale:str, evidence:tuple[str,...]`. **Robust JSON extraction** `_strip_json_fence`: tolerates ```json fences AND surrounding prose (falls back to the outermost `{...}` span) — real models wrap JSON. `_coerce_confidence` clamps to [0,1]; `_coerce_evidence` normalises str→1-tuple / sequence→tuple. **`build_converse_request(prompt, config)`** = pure dict `{modelId, messages:[{role:user, content:[{text}]}], system:[{text}], inferenceConfig:{maxTokens,temperature}}`. **`BedrockConfig`** default `model_id="anthropic.claude-sonnet-4-6"` (just a configurable string, never invoked in tests — latest Claude per CLAUDE.md), region us-east-1, temperature 0.0 (reproducible verdicts). `DEFAULT_SYSTEM_PROMPT` enumerates the 4 typologies (peeling_chain/nested_service/layering_smurfing/consolidation) and demands JSON-only output. `classify()` accepts a JSON string (passthrough) OR a dict (re-`json.dumps` compact). **Test design:** `_StubClient.converse(**kw)` records calls + returns a canned Converse payload; key test monkeypatches `bc.build_default_client` to raise → proves the stub path never constructs a default client. `import ellip2.llm.bedrock_client as bc` so the monkeypatch targets the right name. 14 new tests (parse fields, injected-client-used, no-default-client, string passthrough, request shape, fence/prose tolerance, confidence clamp ×2, evidence-str→tuple, missing-key/non-JSON/malformed/no-text raises). 202 green (was 189), ruff+mypy clean. **For T-020 typology_graph:** `BedrockTypologyClient.classify` (or the pure `parse_converse_response`) is the injectable LLM node; `TypologyResult.typology` is what the rule-based validator checks against structural signals.

- [T-018] `llm/serialize_subgraph.py` (new `llm/` pkg, first LLM-layer module): compact, DETERMINISTIC JSON serialization of a Stage 3 candidate for the Bedrock Converse prompt (plan.md §8). **Input dataclasses** (all frozen): `SubgraphNode(node_id, role, features)`, `SubgraphEdge(source, target, weight, volume, timestamp)`, `CandidateSubgraph(subgraph_id, pu_score, nodes, edges, exit_paths, stats)`. **`SerializationConfig(max_bytes=None, n_bins=10, precision=6)`**. **Two entry points:** `serialize_subgraph(candidate, config) -> dict` (JSON-able, already rounded/sorted/binned) and `serialize_subgraph_json(...) -> str` (compact encode). **Payload schema:** `{subgraph_id, pu_score, n_nodes, n_edges, nodes[{id,role,bins}], edges[{s,t,weight,volume,timestamp}], exit_paths[[ids]], stats{}, truncated}`. **Determinism (THE testable property):** every collection sorted by a stable key (nodes by id, edges by (s,t), stats by key), every float `_round`ed (with -0.0→0.0 normalization), encoded via `json.dumps(sort_keys=True, separators=(",", ":"))` → byte-identical and INPUT-ORDER-INDEPENDENT (test reverses node/edge/stats order → identical bytes). **Feature binning** `_bin_features`: per-feature-column min-max across the subgraph → `floor((v-lo)/(hi-lo)*n_bins)` clamped to `n_bins-1`; constant column (span≤0) → all-zeros (the 43-d anonymized vectors are too large/uninformative verbatim — plan.md "binned features"). **Size-budget shedding (deterministic, never random):** if full encode > `max_bytes`, shed detail in fixed priority — outer loop drops least-important NODES last (priority via `_node_priority`: exit-path nodes FIRST so the corroborating chain survives, then ascending id), inner loop drops EDGES first (kept order = heaviest weight first, ties by endpoints, so the trimmed set is the most-important edges). Sets `truncated=True`; raises ValueError if even the minimal doc can't fit. Edges/exit-paths referencing dropped nodes are filtered out (referential integrity). **Validation:** ValueError on n_bins<1, precision<0, max_bytes≤0, duplicate node_id, edge/exit-path referencing an unknown node. **Gotchas:** edge kept-order (heaviest first) is SEPARATE from output-order (by (s,t)) — kept set chosen by weight, then re-sorted for stable output; dropping one ~50-byte edge object easily clears a 40-byte overshoot so the budget test uses `full_bytes-40`. Pure function, no numpy needed, no network (SIGN-101). 13 new tests (schema, binning span + constant-col, byte-identical + order-independent determinism, rounding, budget-respected+truncated flag, no-budget keeps all, impossible-budget raises, unknown-node raises ×2, invalid-config param). 189 green (was 176), ruff+mypy clean. **For T-019/T-020:** this dict/JSON is the Converse prompt body; `truncated` tells the classifier the doc was trimmed.

- [T-017] `discovery/discover.py` + `scripts/discover.py` shim: the Stage 3 RevFilter corroboration ranker (plan.md §9 + §7). **Module/shim idiom** (mirrors build.py): core `discover_candidates(...)` + `main()` live under `src/ellip2/discovery/`; `scripts/discover.py` is a thin sys.path shim → `discover.main`; the test imports `ellip2.discovery.discover`. **Pipeline = three AND-gates, then rank.** (1) **score gate** `threshold = float(np.quantile(scores, score_percentile))`, `above = scores >= threshold` (percentile→absolute cut-off; `score_percentile=0.9` keeps top 10%). (2) **exit-path gate** — `reachability(edge_index, above_ids, endpoints, n_nodes, max_hops, frontier_cap, hubs)` from T-016; a node passes iff `candidate_reaches` (its BACKWARD distance to the endpoint set ≤ max_hops). Map back via `reaches_by_node = {int(c): bool(r) for c,r in zip(reach.candidates, reach.candidate_reaches, strict=True)}` (candidates is sorted-unique of above_ids, aligned to candidate_reaches). `backward_hops` reported from `reach.backward.hops[nid]`. (3) **typology gate** `typ[nid] >= typology_threshold`; `typology_signal=None` ⇒ zeros + threshold default 0.0 ⇒ no-op (gate passes). Survivors sorted `key=lambda r:(-score, node)` → **descending score, ties by ascending node** (Hit-Rate/NDCG ordering), `rank` 1-based. **`DiscoveryConfig`** (score_percentile=0.9, max_hops=MAX_HOPS, frontier_cap=None, typology_threshold=0.0); **`Candidate`**(node, score, score_pct, backward_hops, typology_signal, rank); **`DiscoveryResult`**(candidates, score_threshold, n_above_threshold, n_reached, max_hops) — n_reached counts score-passing nodes that ALSO reach (before the typology cut), useful for funnel diagnostics. `score_pct` = `np.searchsorted(np.sort(scores), scores, side="right")/n_nodes` (fraction with score ≤ this; the population max → 1.0). **Validation:** ValueError on n_nodes≤0, mis-sized scores/typology, non-finite scores, score_percentile∉[0,1]. **Gotchas:** ruff B905 → `zip(..., strict=True)` (arrays equal-length by construction); mypy runs only on `src` so test-side kw-`object` `# type: ignore` are harmless. **Test (8-node synthetic, endpoint=7, SIGN-101):** edges `0→7, 3→6→7, 2→4→7`, node 1 has NO path; scores top-half→{0,1,2,3}; typology filters node 2 (0.1<0.5). Asserts survivors `[3,0]` ranked, node 1 dropped (no path), node 2 dropped (typology), low-score nodes excluded; `max_hops=1` keeps only the 1-hop `0→7`; `typology=None` re-admits node 2 → `[3,0,2]`; stricter percentile 0.99 → `[3]`. 13 new tests, 176 green (was 163), ruff+mypy clean. **For T-021 report/render:** a `Candidate` carries node/score/score_pct/backward_hops/typology_signal/rank — the ranked lead list a report renders per subgraph.

- [T-016] `exit_paths/path_search.py` was already present (untracked, complete); this iteration added the missing `tests/test_path_search.py` (plan.md decision #4 + §3). **Module API (for downstream T-017):** `reachability(edge_index, sources, endpoints, n_nodes, *, max_hops=MAX_HOPS(=6), frontier_cap=None, hubs=None) -> ReachabilityResult`; lower-level `bfs_reachable(adjacency, seeds, *, max_hops, frontier_cap, hub_mask) -> BFSResult`; `induced_subgraph(edge_index, node_mask) -> (sub_ei, node_ids)`. **Semantics asserted by the test (the 4 acceptance properties):** (1) **reachability within k** — a candidate reaches the endpoint set iff its BACKWARD distance ≤ max_hops; the backward sweep runs on the *transposed* adjacency from endpoints, so `backward.hops[v]` = #hops `v→…→endpoint` (chain 0→1→2→3, endpoint 3 ⇒ back.hops=[3,2,1,0]); max_hops=2 makes the same candidate `candidate_reaches=False` (k is a hard bound). (2) **frontier_cap** — per level keeps the lowest-id `frontier_cap` newly-discovered nodes and appends the level to `capped_levels`; fan-out 0→{1,2,3,4} with cap=2 ⇒ frontier[1]=[1,2], capped_levels=[1], 3&4 unreached. (3) **hubs stopped at** — a reached hub is recorded (`reached`/`hops` set) but NOT expanded, so a route that only reaches the endpoint THROUGH a hub fails (`candidate_reaches=False`); but a hub that is itself a SEED is still expanded (origin exemption). (4) **backward∩forward** — `survivors` = meet-in-the-middle `{m: fwd.hops[m]+back.hops[m] ≤ max_hops}` over nodes reached in BOTH sweeps; a forward-reachable dead-end branch (1→3, 3 reaches no endpoint) is excluded, and a longer alternate route is excluded once total hops exceed max_hops. **Test idiom/gotchas:** `_edge_index`/`_adjacency` helpers from a `[(src,dst)]` list; ruff isort groups `scipy` (third-party) BEFORE `ellip2` (first-party) — scipy import must precede the ellip2 import block. Also removed a stale unused `from collections.abc import Iterable` in path_search.py (ruff F401). Validation: out-of-range endpoint / negative max_hops / `frontier_cap=0` all raise ValueError. 13 new tests, 164 green, ruff+mypy clean.
- [T-015] `eval/leakage_checks.py`: three reusable leakage guards (plan.md §7, SIGN-103), each pure/numpy/deterministic and each raising a custom `LeakageError(Exception)` (NOT `assert` — survives `python -O`; tests use `pytest.raises(LeakageError)`). **(1) `assert_test_labels_masked(exposed_label, node_subgraph, subgraph_in_test)`** — `exposed_label` is a `(N,)` array where nonzero = "node i contributed its observable subgraph label to a feature" (caller sums neighborhood.py's licit+suspicious indicators). Maps each labeled node to `subgraph_in_test[node_subgraph[n]]`; raises if any TEST-subgraph node has nonzero exposure. Background nodes (`node_subgraph==-1`) ignored. Pairs with neighborhood.py's test-split zeroing — verifies the masking actually happened. **(2) `assert_positive_splits_disjoint(**named_splits)`** — keyword form `(prior=..., train=..., recall=...)`, each an iterable of ids (any hashable; dedups via `set`); raises on ANY pairwise overlap (plan.md §7c: SCAR recall must be on unseen positives). `ValueError` if <2 splits. **(3) `assert_membership_not_encoded(features, node_subgraph)`** — over LABELED nodes only, a column "encodes membership" iff it is **constant within every subgraph AND distinct across subgraphs** (= a 1:1 subgraph fingerprint readable off one feature). **Key design choice:** the "constant-within" requirement is what avoids a false positive on near-unique continuous anonymized features — a per-node-unique column is NOT constant within a multi-node subgraph, so it's not flagged; only a literal subgraph-id-like column trips it. A globally-constant column is constant-within but NOT distinct-across → ok. No-ops with <2 subgraphs. 1-D features promoted to a single column. **Test design (clean+leaky per invariant, SIGN-101):** inv1 clean has test nodes exposing 0; leaky has a test node exposing 1 (`match="TEST subgraph"`). inv3 clean has col0 vary within-subgraph + col1 collide across (all 9.0); leaky sets col1==subgraph id (`match="fingerprint"`); plus globally-constant-ok and unlabeled-ignored cases. 13 new tests, 151 green, ruff+mypy clean.
- [T-014] `eval/pu_metrics.py`: the SCAR-safe reporting metrics (plan.md §7; RevClassify `final_test/{f1,prauc}`). **Four pure-numpy fns + `pu_metric_report` bundler**, label convention `1`=suspicious/positive `0`=licit/unlabeled (shared w/ Stage 2). **`pr_auc(scores,labels)`** = average precision `Σ ΔR·P` the sklearn way (NOT trapezoid): sort desc by score (mergesort, stable ties), `tps/fps=cumsum`, collapse to one point per distinct score via `boundaries=r_[where(diff(s))[0], n-1]`, `precision=tps/(tps+fps)`, `recall=tps/n_pos`, anchor at `(R=0,P=1)` then `sum(diff(recall)*precision[1:])`. It's a LOWER BOUND under SCAR (hidden positives in the unlabeled pool depress apparent precision). **`binary_f1`**=`2TP/(2TP+FP+FN)`, **`recall`**=`TP/(TP+FN)` (the one thing SCAR estimates unbiasedly), **`lee_liu_score`**=`recall²/Pr(ŷ=1)` (Lee&Liu 2003 lift — rewards recall, penalises predicting-positive-indiscriminately via the over-ALL-data denom). Threshold-dependent three take **binary preds**; only pr_auc takes raw scores. `pu_metric_report(scores,labels,*,threshold=0.5)` thresholds `scores>=threshold` → dict keyed `COLUMNS=("prauc","f1","recall","lee_liu")`. **Degenerate guards (return 0.0, don't raise):** f1 when `2TP+FP+FN==0`, recall when no positives, lee_liu when no predicted positives; pr_auc DOES raise on zero positives or length mismatch. **`_as_binary`** rejects non-0/1 (`array_equal` after int64 cast catches floats like 0.5). **Clean hand values for tests:** scores`[.9,.8,.7,.6]`/labels`[1,0,1,0]`→AP=**5/6**; yt`[1,0,1,0,1]`/yp`[1,0,1,1,0]` (TP2,FP1,FN1)→f1=recall=**2/3**, lee_liu=`(4/9)/(3/5)`=**20/27**; selective`[1,1,0,0]`→2.0 > indiscriminate`[1,1,1,1]`→1.0 on yt`[1,1,0,0]` (proves the over-prediction penalty); tied `[.5,.5,.5,.5]`→0.5 (single boundary). No sklearn dep (not in pyproject) — all closed-form. 14 new tests, 138 green, ruff+mypy clean.
- [T-013] `pu/trainer.py`: supervised subgraph baseline + cluster-level nnPU head + MIL max-pool (plan.md decisions #1/#2, §"Subgraph-level readout"). **Two framings, one module.** (a) `SupervisedSubgraphModel` = the DEFAULT RevClassify_DS comparator: `DeepSets(senders) ⊕ DeepSets(receivers)` (border nodes) concat with `[sum,mean,max]`-pooled internal node(43-d)+edge(95-d) feats → MLP → 1 logit; trained by `train_supervised(...)` with **weighted BCE** (`binary_cross_entropy_with_logits(pos_weight=...)`). Subgraph level = supervised (reliable licit negatives), NOT PU. (b) `ClusterScorer(encoder, emb_dim)` wraps the T-011 `HeterophilyEncoder` + linear head → `(N,)` cluster logits; `train_cluster_nnpu(...)` uses the T-009 nnPU risk. **Kiryo training rule wired correctly:** backward pass uses `nnpu_risk_for_backward` (ascends the over-fit negative term when the clamp engages) while the logged `TrainHistory.losses` records the value-correct `nnpu_risk` — so "loss decreases" is asserted on the REPORTED risk, not the ascent surrogate. `nnpu_risk` returns `Tensor|tuple`, so narrow with `assert isinstance(value, Tensor)` (avoids a type-ignore) before `float()`. **`DeepSets`** = φ-MLP → multi-stat pool → ρ-MLP (Zaheer 2017); **`_segment_pool(x, batch, num_segments, ops)`** is the shared scatter-pool — `index_add_` for sum/mean (mean divides by `counts.clamp(min=1)`), `scatter_reduce(reduce="amax", include_self=True)` over a `-inf` init for max, then `torch.where(isfinite, m, 0)` so EMPTY sets/segments pool to 0 (not NaN/inf) — a subgraph with zero border senders is valid. **`max_pool_to_subgraph(scores, member_subgraph, num_subgraphs, *, empty_value=0)`** = MIL/noisy-OR (bag positive iff ≥1 positive member): same `scatter_reduce amax` + `-inf`→empty_value trick; hand example `[0.1,0.9,0.3 | 0.2,0.05 | 0.7 | (none)]`→`[0.9,0.2,0.7,0.0]`. **Checkpointing:** `save_checkpoint`/`load_checkpoint` persist `{model_state, optimizer_state, extra}` via `torch.save`; `load_checkpoint(..., weights_only=False)` (default flipped in newer torch — our payload has non-tensor `extra`). Both train fns accept an `optimizer=` to RESUME, which is what makes the round-trip testable: train→save→fresh model+opt→load→continue N steps on both → IDENTICAL loss trajectory (proves optimizer Adam moments+step counters restored, not just weights). `SupervisedSubgraphModel` has NO dropout, so train/eval are deterministic → the trajectory-equality assertion is exact. **Test design (CPU, synthetic, SIGN-101):** `_make_batch(labels)` encodes the label in the BORDER sender features (±3 shift) so weighted-BCE separates them and loss drops over 80 epochs; nnPU uses a 12-node graph, positives(+3)/unlabeled(−3) features with class-internal edges so the encoder doesn't smooth them together, prior=0.3, 100 epochs, risk drops. **Gotcha:** `scatter_reduce` needs the index expanded to the value shape (`batch.unsqueeze(1).expand_as(x)`) for the 2-D max pool; the 1-D `max_pool_to_subgraph` indexes directly. 124 tests green (7 new), ruff+mypy clean.
- [T-012] `graph/neighbor_sampling.py`: candidate-set neighbor sampling (plan.md §3). **Blocker discovered:** PyG's `NeighborLoader` sampling kernel REQUIRES `pyg-lib` or `torch-sparse` (C++ ext) — neither is installable in this env (torch 2.12.1+cpu is too new for prebuilt PyG companion wheels, and from-source build needs `--no-build-isolation` + a compiler and likely ABI-fails). So iterating a real `NeighborLoader` raises `ImportError` on CPU → can't be the test path (SIGN-101). **Two-layer design:** (1) `build_neighbor_loader(data, input_nodes, config)` = the production/GPU path, a thin wrapper that fixes our conventions (default fanout `[15,10]`, `input_nodes`=candidate seeds) onto a real `NeighborLoader`. Test asserts its *configuration* without iterating: `loader.batch_size`, `loader.node_sampler.num_neighbors.values == [15,10]`, `loader.input_data.node` (these are the introspectable attrs — there is NO `loader.num_neighbors`). (2) `sample_subgraph(edge_index, seeds, num_neighbors, *, num_nodes, replace, generator)` = a pure-`torch` kernel-free reference sampler reproducing the SAME fanout-capped directional semantics, so the cap/seed logic actually runs on CPU. Plus `iter_subgraph_batches(data, input_nodes, config)` to mini-batch seeds through it. **Direction convention (matches pyg_data):** col `[s,t]`=`s→t`, flow source_to_target, so a seed `v` aggregates from its **in-neighbors** `{s:(s→v)}` — sampling draws in-neighbors (`src[dst==v]`). **Fanout-cap semantics made testable:** a node's in-degree IN THE SAMPLED subgraph == #neighbors drawn for it == ≤ cap at the hop it was expanded. Seed expanded at hop0 (≤cap[0]); hop-1 nodes at hop1 (≤cap[1]); last-hop nodes never expanded (in-deg 0). So the test asserts `in_deg(seed_local) ≤ cap[0]` and `in_deg(other_local) ≤ cap[1]` exactly. `replace=True` uses `torch.randint` → pads to `cap` even with fewer in-neighbors (a 2-in-neighbor node yields 3 edges); `replace=False` uses `randperm[:cap]` → exactly `min(cap, avail)`. `SampledBatch(n_id, batch_size, edge_index(local-relabeled), num_nodes)` mirrors NeighborLoader's layout (seeds first in `n_id`, `seed_n_id`=`n_id[:batch_size]`). `NeighborSamplingConfig(num_neighbors=(15,10), batch_size=512, shuffle, replace)` validates in `__post_init__` (non-empty, ≥0 fanout, batch_size≥1). **Gotchas:** `field` import was unused (ruff F401) — drop it; frontier de-dup via `dict.fromkeys` so each node expands once per hop. 12 new tests (seed preservation, exact cap, cap-when-fewer, 2-hop per-node cap, replace-pads, isolated seed→0 edges, determinism, mini-batching, loader config, validation). 117 tests green, ruff+mypy clean.
- [T-011] `pu/encoder.py`: heterophily-tolerant GNN encoder (plan.md §2/§3). **Core layer** `EgoNeighborConv(MessagePassing, aggr="mean")`: `h_v' = W_ego·x_v + W_neigh·AGG_{u∈N(v)} x_u` with TWO independent linear maps and **NO self-loops** — the ego path (`lin_ego`, with bias) bypasses aggregation entirely so a node's own features are preserved against a class-mismatched (heterophilous) or empty neighborhood. This is the GraphSAGE/H2GCN "never mix ego into neighbor agg" fix vs pure-GCN smoothing (which folds self-loops into one symmetric-normalized mean). `lin_neigh` is **bias-free** on purpose → an isolated node (no in-neighbors) gets `propagate→0` so its neighbor contribution is EXACTLY 0 and output == `lin_ego(x_iso)` (the key testable property). `message(x_j)=x_j` (source-node features; PyG flow source_to_target, edge col `[s,t]`=`s→t` so `t` aggregates from in-neighbor `s`). **Encoder** `HeterophilyEncoder(in,hidden,out=None,*,num_layers=2,aggr,dropout,activation=relu,normalize,bias)`: stacks layers, activation+dropout after every layer EXCEPT the last (last stays linear so the PU head sees raw embeddings), optional final L2 `F.normalize`. `dims=[in]+[hidden]*(num_layers-1)+[out]`; `num_layers=1` → single `in→out` map (out defaults to hidden). `num_layers<1` raises ValueError. **mypy gotcha:** iterating `nn.ModuleList` yields `Module` whose `__getattr__` is typed `Tensor|Module`, so `conv.reset_parameters()` errors "Tensor not callable" — `cast(EgoNeighborConv, conv).reset_parameters()` fixes it (calling `conv(...)` via `__call__` is fine, no cast needed). **Test design:** tiny 6-node ring + 1 deliberately ISOLATED node (idx 5); asserts output shape `(N,out)`, `loss.backward()` populates finite grads on EVERY param (no dead paths), isolated-node embedding == `lin_ego(x_iso)` and changes iff its OWN features change (and can't affect others — no edges), zeroing `lin_neigh.weight` → pure per-node ego transform (proves the two paths are separate), and `normalize=True` → unit-norm rows. CPU torch, `torch.manual_seed(0)`. 105 tests green (7 new), ruff+mypy clean.
- [T-010] `pu/prior_estimation.py`: TIcE class-prior estimator (Bekker & Davis 2018, AAAI) in the **SCAR** framing + `prior_sweep_grid`. **Framing:** single dataset with a boolean `labeled` mask (`s=1` = labeled positive, `s=0` = unlabeled); estimates `c=P(s=1|y=1)` then `π̂_p = γ/ĉ` where `γ=P(s=1)`. **TIcE primitive:** within ANY subdomain T the labeled fraction `#lab_T/#tot_T ≤ #lab_T/#pos_T = c` (since `#pos_T ≤ #tot_T`), so every subdomain's labeled fraction is a LOWER bound on c; `ĉ = max_T (labeled_frac − correction)`, tightest on a (near-)pure-positive region. **Subdomain induction** `_subdomain_masks`: small greedy decision tree — each node splits on the single feature/median-threshold whose child is richest in labeled examples; both children become candidates + recursed to `max_depth` (default 4); full domain always a candidate. **Key bound used twice:** `c ≥ γ` always (full sample is a valid subdomain) → floor `ĉ` at γ, which guarantees `π̂_p = γ/ĉ ∈ (0,1]` (no upper clamp needed, naturally ≤1). **delta** (default 0.0 = no correction) applies a Hoeffding downward correction `sqrt(ln(1/δ)/(2|T|))` to each subdomain's frac → smaller ĉ → LARGER (more conservative) π̂_p; assert `conservative.prior ≥ plain.prior`. `_C_FLOOR=1e-6` guards the div. **`prior_sweep_grid(lo=1e-5,hi=1e-1,num=5)`** = `np.geomspace` (one point per decade by default → [1e-5..1e-1]), `include=` merges a point (e.g. the estimate) via `np.unique`; validates 0<lo<hi<1, num≥2. plan.md §2: π_p is the small CLUSTER-level prior, NOT the 2.27% subgraph rate; heterophily biases it up so we SWEEP both directions. **Test design:** separable SCAR mixture pos~N(+5,1)/neg~N(−5,1), seeded RNG → a `feature>0`-ish subdomain is ~pure-positive so labeled_frac there ≈ c; π=0.25,c=0.6 recovered within abs 0.06 (γ≈0.15). All pure-numpy, `npt.NDArray` typing. 104 tests green (6 new), ruff+mypy clean.
- [T-009] `pu/nnpu_loss.py` (new `pu/` pkg, first PU module): non-negative PU risk (Kiryo 2017, arXiv:1703.00593). **Surrogate** `sigmoid_loss(logits, positive: bool)` = σ(−t·z): `positive=True`→σ(−z)=ℓ(·,+1), `False`→σ(z)=ℓ(·,−1); symmetry ℓ⁺+ℓ⁻=1 (asserted) is why sigmoid is the recommended PU surrogate. **Core** `_risk_parts(p_logits, u_logits, prior, *, surrogate, beta)` → frozen `PURiskParts(positive_risk=π_p·E_P[ℓ⁺], negative_risk=E_U[ℓ⁻]−π_p·E_P[ℓ⁻] (UNBIASED, pre-clamp), clamped: bool)`. Public: `nnpu_risk(...)` returns `positive_risk + clamp(negative_risk, min=−beta)` as a 0-dim grad tensor (default beta=0 → plain non-negativity; `return_parts=True` → `(risk, parts)`); `upu_risk(...)` = unbiased (no clamp) for comparison; `nnpu_risk_for_backward(..., gamma=1.0)` = Kiryo training rule (when clamped, returns `−γ·negative_risk` so backward ASCENDS the over-fit term; else == unbiased). **Key facts:** π_p is the small CLUSTER-level prior, NOT the 2.27% subgraph rate (Resolved decision #2). `prior∈(0,1)` and non-empty P & U enforced (ValueError). `surrogate: SurrogateLoss = Callable[[Tensor,bool],Tensor]` is injectable. **Test design for the clamp:** to force `negative_risk<0`, make E_U[ℓ⁻] small (U logits strongly negative, σ(z)≈0) AND π_p·E_P[ℓ⁻] large (P logits strongly positive, σ(z)≈1) with π_p=0.5 → clamp engages, risk==positive_risk, and upu_risk dips strictly below. Hand-value test uses the well-separated regime (negative term >0) so nnPU==unbiased and a pure-Python σ re-derivation matches. **Gotchas:** ruff UP035 → import `Callable` from `collections.abc` not `typing`; torch already in mypy override list (ignore_missing_imports) so no stub work. 99 tests green (7 new), ruff+mypy clean.
- [T-008] `graph/pyg_data.py` (new `graph/` pkg, first Stage-2 module): `build_pyg_data(artifacts_dir, *, mmap_mode="r") -> torch_geometric.data.Data` — **homogeneous** Data (single node type, single edge type; NOT HeteroData — subgraph labels are a membership overlay, not a 2nd node type). `x` float32 `(N,43)`, `edge_index` int64/**long** `(2,E)` (PyG requires long), `data.num_nodes=N` set explicitly so isolated nodes still count. **Loading:** `load_node_features`/`load_edge_index` use `np.load(..., mmap_mode=...)` (default `'r'`) → returns `np.memmap` (read-only, `.flags.writeable False`). **Tensor materialization:** `torch.from_numpy(np.ascontiguousarray(memmap, dtype=...))` — ascontiguousarray COPIES the read-only memmap into owned writable RAM with the target dtype, which (a) gives PyG the float32/int64 it wants and (b) avoids the "given numpy array is not writable" warning a bare `from_numpy(memmap)` emits. **Subgraph accessors:** `SubgraphMembership(ccids, labels, members)` frozen dataclass from `subgraphs.parquet` via `load_subgraph_membership` (pyarrow, `member_idx` lists → int64 arrays). Methods: `member_idx(sid)`, `index_of(ccid)` (raises KeyError, not ValueError, on miss), `members_of(ccid)`, and `node_subgraph(n_nodes)` = dense inverse (`-1` for unlabeled bg nodes) with `node_subgraph[members[s]]==s` (the round-trip the test asserts). Mirrors `neighborhood.load_subgraph_labels`'s parquet-row-order subgraph indexing. **mypy gotcha:** `np.load`'s `mmap_mode` is typed `Literal['r','r+','w+','c']|None`, so a plain `str|None` param fails — declared `MmapMode = Literal[...] | None` alias. Test reuses `test_ingest._write_raw/_cfg`+real `ingest()` for artifacts (no torch_geometric type stubs, but `ignore_missing_imports`+per-module override cover it). 85 tests green, ruff+mypy clean.
- [T-007] `features/build.py` + `scripts/build_features.py`: `build_cluster_features(FeatureBuildConfig) -> BuildResult(out_path, n_nodes, columns)` assembles T-001..T-006 into one `cluster_features.parquet` (one row per cluster idx; first column `idx = arange(N)`). **Config** carries anonymized-column choices (all default 0/(0,1,2)): `weight_index`, `timestamp_index`, `size_index` (node-feature col for #addresses), `edge_agg_indices`; plus `artifacts_dir` (Stage 0 out), `raw_dir` (for `background_edges.csv`), `split_csv` (persisted split). **Data routing:** degree+neighborhood read the canonical `edge_index.npy` memmap (topology only, no CSV); edge aggregates via `compute_edge_aggregates_duckdb` (streamed GROUP BY, SIGN-104); flow_concentration+temporal are pure-numpy so they need per-edge arrays — `_pull_edge_columns` does ONE DuckDB join (`background_edges` ⋈ `id_map` twice) returning a rebuilt `(2,E)` ei + only the requested weight/timestamp columns (NOT all 95). **Key correctness fact:** every feature builder is permutation-invariant in edge order, so the DuckDB-rebuilt ei (carrying the pulled cols) and `edge_index.npy` describe the same edge set and agree — that's why mixing the two sources is safe. `throughput` for path_role = `bincount(src,w)+bincount(dst,w)` (incident weight sum); `size` = `node_features[:, size_index]`. **Finiteness guard** raises on any NaN/inf before writing. CLI shim mirrors `make_split.py` (delegates to `build.main`). **Test gotchas:** reuse `test_ingest._write_raw/_cfg/N` + real `ingest()` to make artifacts, then hand-write `split.csv` (cols id,label,split; mark S1+L2 `test`). Synthetic edge feats are all 0.0 → weights/timestamps 0 → flow/temporal empty_value=0, throughput 0 → path_role throughput pct all 0.5 (fine, still finite). Leakage test flips `subgraph_in_test` off and asserts ≥1 neighborhood fraction changes (proves split consumed). `import duckdb`/`pyarrow` inside funcs (mypy/idiom). 80 tests green, ruff+mypy clean.
- [T-006] `features/path_role.py`: `compute_path_role(in_degree, out_degree, size, throughput)` → dict keyed by `COLUMNS = ("endpoint_score", "source_score", "source_sink_axis")`, all float64 (N,). **Pure ranking module** — takes already-materialized per-cluster signals (degree from T-001, plus size=#addresses and throughput=summed flow) and only ranks+combines; no edge_index. **Calibration by PERCENTILE** (Resolved decision #3): each channel → in-population percentile rank via `_percentile_rank` (avg ordinal rank over ties / (n−1); smallest→0, largest→1, single node→0.5, all-equal→0.5). Monotone non-decreasing, so adding signal can only raise a score (Stage 3 relies on this; tested). `endpoint_score = mean(pct(in_degree), pct(size), pct(throughput))` ∈[0,1] (exchange/licit-sink likelihood); `source_score = mean(pct(out_degree), pct(size), pct(throughput))` ∈[0,1]; `source_sink_axis = pct(in_degree) − pct(out_degree)` ∈[−1,1] (+1 sink-leaning, −1 source-leaning). **Docstring loudly flags these as DERIVED heuristics, not ground-truth entity types** (dataset ships no node/entity labels). Clean hand graph: 5 clusters strictly graded sink→source so ranks are tie-free → endpoint_score [1,.75,.5,.25,0], axis [1,.5,0,−.5,−1]. `_percentile_rank` uses `np.unique(return_inverse,return_counts)`; `inv.ravel()` (numpy 2 returns 2-D inverse). No empty_value param needed — percentile is always defined for n≥1. 9 tests, gate green (74 total, ruff+mypy clean).
- [T-005] `features/temporal.py`: `compute_temporal_features(edge_index, timestamps, n_nodes, *, empty_value=0.0)` → dict keyed by `COLUMNS = ("activity_span", "burstiness")`, both float64 (N,). **Events = incident edges** (direction-agnostic): each edge is one event for BOTH endpoints via `node=concat(src,dst)`, `event_ts=concat(ts,ts)` — so incident-event count == `total_degree`, and a self-loop yields two same-ts events. `timestamps` is the 2nd POSITIONAL arg (like `weights` in flow_concentration), length E. **activity_span** = per-node `max−min` via `np.minimum.at`/`np.maximum.at` (init ±inf); genuine `0.0` for a single event, `empty_value` only when zero events. **burstiness** = Goh&Barabási `B=(σ−μ)/(σ+μ)` over inter-event GAPS, B∈[−1,1] (−1 regular/periodic, 0 Poisson, →1 bursty). Vectorised gaps: `order=lexsort((event_ts,node))`; gap between consecutive same-node sorted events (`sn[1:]==sn[:-1]`) belongs to `sn[1:]`, value `st[1:]-st[:-1]` (≥0). **Population** std (matches edge_aggs): `var=Σg²/n − μ²`, clip≥0 for fp. **empty_value** for burstiness when <2 events (no gap) OR all events share one ts (`σ+μ==0`, denom 0). **Clean hand values for tests:** equal gaps→−1; single gap (2 events)→−1; gaps[0,2]→0; gaps[1,9] (μ5,σ4)→−1/9; k zeros + one spike → `(√k−1)/(√k+1)` (k=4 → 1/3, a clean bursty>0). Test isolates each measured source node (0..7) with its own out-edges to dedicated sinks (idx≥10) so incident sets are controlled; node 7 mixes one in + one out edge to assert incident = in∪out. 14 tests, gate green (65 total, ruff+mypy clean).
- [T-004] `features/neighborhood.py`: `compute_neighborhood_features(edge_index, node_subgraph, subgraph_label, subgraph_in_test, n_nodes, *, hops=(1,2), empty_value=0.0)` → dict keyed `hop{h}_frac_{licit,suspicious,unknown}` (float64 (N,), sum to 1 on non-empty neighbor sets), plus loader `load_subgraph_labels(subgraphs_parquet, split_csv, n_nodes)` → `SubgraphLabels(node_subgraph, subgraph_label, subgraph_in_test, ccids)`. **Label codes:** `LICIT=0, SUSPICIOUS=1`; `node_subgraph[i] = -1` for unlabeled background nodes (most of the 49M). **Graph is symmetrised** (undirected) — label homophily is direction-agnostic. `hop1` = immediate neighbors; `hop2` = ring at shortest distance *exactly* 2 (distance-1 + self removed). Denominator = full neighbor-set size, so masked neighbors stay in as `unknown`. **Leakage (SIGN-103), two masks:** (1) own-subgraph — same-subgraph neighbors carry v's own label, removed via `lic = A@lic_ind - same*lic_ind` where `same[v]=(A@M).multiply(M).sum(1)` is the same-subgraph neighbor count and `lic_ind[v]` (the node's own observable indicator) IS the self-mask since members share it; (2) test-split — globally zero a node's licit/suspicious indicator if its subgraph is in the persisted TEST split. **scipy.sparse:** boolean csr adjacency; 2-hop = `(A@A>0)` minus `(A@A>0).multiply(A)` (intersection only, no dense N×N) then `setdiag(0)`. **Gotchas:** K=0 (all-unlabeled) breaks fancy-indexing `labels[ns[valid]]` — guard with `if labels.size`. Test nodes still GET features from their non-test neighbors (only their OWN label is withheld downstream). Leakage tests use positive controls (flip mask off → fraction changes) so silent un-masking fails loudly. 51 tests green, ruff+mypy clean.

- [T-003] `features/flow_concentration.py` was already present (untracked); this iteration added the missing `tests/test_flow_concentration.py`. API: `compute_flow_concentration(edge_index, weights, n_nodes, *, empty_value=0.0)` → dict keyed by `COLUMNS` = `{in,out}_{gini,hhi,max_counterparty_share}`, all float64 (N,). **Conventions (reuse):** out-flow grouped by source (row0), in-flow by target (row1); per-counterparty weights are SUMMED first (parallel edges merge into one share) so Gini/HHI/share are consistent. Gini via sorted-rank closed form `Σ(2·rank−n−1)·w_(i) / (n·S)`, clipped to [0,1]; HHI=Σ(w_i/S)²; share=max w_i/S. Empty direction (S==0) → `empty_value`. **Test gotchas:** `weights` is the 2nd POSITIONAL arg (before `n_nodes`) — don't pass it as kw after a positional n_nodes. Gini ceiling for one-dominant is (n−1)/n, so "near 1" only holds with many counterparties (n=4 dominant → 0.72, not ~1); assert exact hand value + monotone-vs-equal instead. Hand graph: equal-after-merge (Gini 0), single counterparty (Gini 0/HHI 1/share 1), one-dominant-of-4 (Gini 0.72/HHI 0.9412/share 0.97). 40 tests green, ruff+mypy clean.

- [T-002] `features/edge_aggs.py`: two entry points sharing one column convention. `compute_edge_aggregates(edge_index, edge_features(E,F), n_nodes, *, feature_indices, feature_names, stats=("sum","mean","max","std"), empty_value=0.0)` → numpy in-memory path (test). `compute_edge_aggregates_duckdb(edges_csv, id_map_parquet, n_nodes, ...)` → out-of-core: one `GROUP BY` per direction over `read_csv_auto(edges)` JOIN `read_parquet(id_map)` (SIGN-104; mirrors ingest's `_q`/`_qi`/`to_arrow_reader`). **Conventions:** out-edges grouped by src (row0), in-edges by dst (row1). Columns named `{direction}_{feature}_{stat}` (e.g. `out_ef_0_sum`), all float64 (N,). **std is POPULATION std** (group of 1 → 0.0); DuckDB side uses `stddev_pop` to match. numpy std via `sumsq/count - mean²` clipped at 0 for fp. Nodes with no edge in a direction get `empty_value` (default 0.0, distinct from a genuine zero sum). Test asserts hand-computed in/out aggregates AND that the DuckDB path matches numpy (tiny synthetic CSV + id_map). Gotcha: `feature_names` length must equal `feature_indices` (default indices = ALL columns), so pass both together for a subset.
- [T-001] `features/degree.py`: `compute_degree_features(edge_index, n_nodes, *, zero_denom_value=0.0)` → dict of (N,) arrays keyed by `COLUMNS = (in_degree, out_degree, total_degree, in_out_ratio)`. Pure-numpy `bincount` (row0=src→out_degree, row1=dst→in_degree). **Convention to reuse:** feature modules return `dict[str, np.ndarray]` (column→(N,) array) + a `COLUMNS` tuple, so T-007 can join by name. `in_out_ratio` is undefined when out_degree==0 → filled with `zero_denom_value` (default 0.0, finite, no NaN/inf) — kept distinct from a *genuine* 0.0 ratio (in=0,out>0). Tests use a hand-counted 6-node digraph (pure sink + isolated node) and assert the zero-denom fill vs genuine-zero separately. Int cols are int64, ratio float64.

## Publishing batch (T-031..T-036)
All doc numbers are authoritative in **plans/publishing_facts.md** — READ IT FIRST before writing README/RESULTS; do not invent metrics. Docs must keep `bash scripts/verify.sh` green and satisfy tests/test_docs.py.
